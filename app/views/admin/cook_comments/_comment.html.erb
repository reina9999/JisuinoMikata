<br>
<% cook.cook_comments.each do |cook_comment| %>
  <!--<%= cook.customer.name %>
  <%#= link_to cook_comment.customer.name,admin_customer_path(comment.customer) %>#コメント投稿者-->
  <%= cook_comment.created_at.strftime("%Y-%m-%d %H:%M:%S") %><%= cook_comment.comment %>
  <%= link_to "削除", cook_cook_comment_path(cook_comment.cook, cook_comment), method: :delete,remote: true,class: "btn btn-danger pull-right" %>
<% end %>