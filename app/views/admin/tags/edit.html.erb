<h3>タグ編集</h3>

<%= form_with model: @tag, url: admin_tags_path,method: :patch do |f| %>
  <td><%= f.label :"タグ名", class: "mr-3" %></td>
  <td><%= f.text_field :name, class: "mr-5" %></td>
  <td><%= f.submit "変更を保存", class: "btn btn-primary" %></td>
<% end %>